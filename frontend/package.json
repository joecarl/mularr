{
	"name": "mularr-frontend",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite --host 0.0.0.0",
		"build": "npm run postinstall && tsc && vite build",
		"preview": "vite preview",
		"postinstall": "chispa-cli --compile-html"
	},
	"dependencies": {
		"chispa": "^0.10.2",
		"lucide-static": "^1.25.0"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vite": "^8.0.16"
	}
}